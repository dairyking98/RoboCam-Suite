# RoboCam-Suite Python Dependencies
# Python 3.7+ required

# Camera and Imaging
picamera2>=0.3.12
opencv-python>=4.5.0  # Required for capture interface video encoding (FFV1, MJPG codecs)
# Note: simplejpeg will be installed in venv by setup.sh to match venv's numpy version

# Serial Communication
pyserial>=3.5

# GPIO Control (Raspberry Pi only)
# Note: RPi.GPIO is installed via system package manager on Raspberry Pi
# For development on non-Raspberry Pi systems, this can be skipped
# RPi.GPIO>=0.7.1

# Standard library dependencies (no installation needed):
# - tkinter (usually included with Python)
# - json
# - threading
# - time
# - re
# - csv
# - os
# - sys

